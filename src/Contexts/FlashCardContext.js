import React from "react";

/**
 * FlashCardContext
 * 
 * Returns: The main context provider used to pass data from the app to other components.
 */
const FlashCardContext = React.createContext();
export default FlashCardContext;